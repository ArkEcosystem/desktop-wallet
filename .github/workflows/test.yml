name: Test

on:
    push:
        branches:
            - "master"
            - "develop"
    pull_request:
        types: [ready_for_review, synchronize, opened]

jobs:
    domains-contact:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --frozen-lockfile

            - name: Rebuild
              run: npm rebuild

            - name: Format
              run: |
                  export PATH="$(yarn global bin):$PATH"
                  yarn global add rustywind
                  rustywind --write src

            - name: Check Changes
              id: changes
              run: |
                  git restore '*.test.tsx.snap'
                  if [[ -z "$(git status --porcelain)" ]];
                  then
                    echo "::set-output name=changes::0"
                  else
                    echo "::set-output name=changes::1"
                  fi

            - name: Update Snapshots
              if: steps.changes.outputs.changes == 1
              run: yarn test --maxWorkers=4 --update-snapshot

            - uses: stefanzweifel/git-auto-commit-action@v4
              with:
                  commit_message: "style: resolve style guide violations"
                  branch: ${{ github.head_ref }}

            - name: Test
              run: yarn test:coverage src/domains/contact --forceExit --maxWorkers=4 --coverageDirectory .coverage/unit

    domains-dashboard:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --frozen-lockfile

            - name: Rebuild
              run: npm rebuild

            - name: Format
              run: |
                  export PATH="$(yarn global bin):$PATH"
                  yarn global add rustywind
                  rustywind --write src

            - name: Check Changes
              id: changes
              run: |
                  git restore '*.test.tsx.snap'
                  if [[ -z "$(git status --porcelain)" ]];
                  then
                    echo "::set-output name=changes::0"
                  else
                    echo "::set-output name=changes::1"
                  fi

            - name: Update Snapshots
              if: steps.changes.outputs.changes == 1
              run: yarn test --maxWorkers=4 --update-snapshot

            - uses: stefanzweifel/git-auto-commit-action@v4
              with:
                  commit_message: "style: resolve style guide violations"
                  branch: ${{ github.head_ref }}

            - name: Test
              run: yarn test:coverage src/domains/dashboard --forceExit --maxWorkers=4 --coverageDirectory .coverage/unit

    domains-error:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --frozen-lockfile

            - name: Rebuild
              run: npm rebuild

            - name: Format
              run: |
                  export PATH="$(yarn global bin):$PATH"
                  yarn global add rustywind
                  rustywind --write src

            - name: Check Changes
              id: changes
              run: |
                  git restore '*.test.tsx.snap'
                  if [[ -z "$(git status --porcelain)" ]];
                  then
                    echo "::set-output name=changes::0"
                  else
                    echo "::set-output name=changes::1"
                  fi

            - name: Update Snapshots
              if: steps.changes.outputs.changes == 1
              run: yarn test --maxWorkers=4 --update-snapshot

            - uses: stefanzweifel/git-auto-commit-action@v4
              with:
                  commit_message: "style: resolve style guide violations"
                  branch: ${{ github.head_ref }}

            - name: Test
              run: yarn test:coverage src/domains/error --forceExit --maxWorkers=4 --coverageDirectory .coverage/unit

    domains-exchange:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --frozen-lockfile

            - name: Rebuild
              run: npm rebuild

            - name: Format
              run: |
                  export PATH="$(yarn global bin):$PATH"
                  yarn global add rustywind
                  rustywind --write src

            - name: Check Changes
              id: changes
              run: |
                  git restore '*.test.tsx.snap'
                  if [[ -z "$(git status --porcelain)" ]];
                  then
                    echo "::set-output name=changes::0"
                  else
                    echo "::set-output name=changes::1"
                  fi

            - name: Update Snapshots
              if: steps.changes.outputs.changes == 1
              run: yarn test --maxWorkers=4 --update-snapshot

            - uses: stefanzweifel/git-auto-commit-action@v4
              with:
                  commit_message: "style: resolve style guide violations"
                  branch: ${{ github.head_ref }}

            - name: Test
              run: yarn test:coverage src/domains/exchange --forceExit --maxWorkers=4 --coverageDirectory .coverage/unit

    domains-network:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --frozen-lockfile

            - name: Rebuild
              run: npm rebuild

            - name: Format
              run: |
                  export PATH="$(yarn global bin):$PATH"
                  yarn global add rustywind
                  rustywind --write src

            - name: Check Changes
              id: changes
              run: |
                  git restore '*.test.tsx.snap'
                  if [[ -z "$(git status --porcelain)" ]];
                  then
                    echo "::set-output name=changes::0"
                  else
                    echo "::set-output name=changes::1"
                  fi

            - name: Update Snapshots
              if: steps.changes.outputs.changes == 1
              run: yarn test --maxWorkers=4 --update-snapshot

            - uses: stefanzweifel/git-auto-commit-action@v4
              with:
                  commit_message: "style: resolve style guide violations"
                  branch: ${{ github.head_ref }}

            - name: Test
              run: yarn test:coverage src/domains/network --forceExit --maxWorkers=4 --coverageDirectory .coverage/unit

    domains-news:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --frozen-lockfile

            - name: Rebuild
              run: npm rebuild

            - name: Format
              run: |
                  export PATH="$(yarn global bin):$PATH"
                  yarn global add rustywind
                  rustywind --write src

            - name: Check Changes
              id: changes
              run: |
                  git restore '*.test.tsx.snap'
                  if [[ -z "$(git status --porcelain)" ]];
                  then
                    echo "::set-output name=changes::0"
                  else
                    echo "::set-output name=changes::1"
                  fi

            - name: Update Snapshots
              if: steps.changes.outputs.changes == 1
              run: yarn test --maxWorkers=4 --update-snapshot

            - uses: stefanzweifel/git-auto-commit-action@v4
              with:
                  commit_message: "style: resolve style guide violations"
                  branch: ${{ github.head_ref }}

            - name: Test
              run: yarn test:coverage src/domains/news --forceExit --maxWorkers=4 --coverageDirectory .coverage/unit

    domains-plugin:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --frozen-lockfile

            - name: Rebuild
              run: npm rebuild

            - name: Format
              run: |
                  export PATH="$(yarn global bin):$PATH"
                  yarn global add rustywind
                  rustywind --write src

            - name: Check Changes
              id: changes
              run: |
                  git restore '*.test.tsx.snap'
                  if [[ -z "$(git status --porcelain)" ]];
                  then
                    echo "::set-output name=changes::0"
                  else
                    echo "::set-output name=changes::1"
                  fi

            - name: Update Snapshots
              if: steps.changes.outputs.changes == 1
              run: yarn test --maxWorkers=4 --update-snapshot

            - uses: stefanzweifel/git-auto-commit-action@v4
              with:
                  commit_message: "style: resolve style guide violations"
                  branch: ${{ github.head_ref }}

            - name: Test
              run: yarn test:coverage src/domains/plugin --forceExit --maxWorkers=4 --coverageDirectory .coverage/unit

    domains-profile:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --frozen-lockfile

            - name: Rebuild
              run: npm rebuild

            - name: Format
              run: |
                  export PATH="$(yarn global bin):$PATH"
                  yarn global add rustywind
                  rustywind --write src

            - name: Check Changes
              id: changes
              run: |
                  git restore '*.test.tsx.snap'
                  if [[ -z "$(git status --porcelain)" ]];
                  then
                    echo "::set-output name=changes::0"
                  else
                    echo "::set-output name=changes::1"
                  fi

            - name: Update Snapshots
              if: steps.changes.outputs.changes == 1
              run: yarn test --maxWorkers=4 --update-snapshot

            - uses: stefanzweifel/git-auto-commit-action@v4
              with:
                  commit_message: "style: resolve style guide violations"
                  branch: ${{ github.head_ref }}

            - name: Test
              run: yarn test:coverage src/domains/profile --forceExit --maxWorkers=4 --coverageDirectory .coverage/unit

    domains-setting:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --frozen-lockfile

            - name: Rebuild
              run: npm rebuild

            - name: Format
              run: |
                  export PATH="$(yarn global bin):$PATH"
                  yarn global add rustywind
                  rustywind --write src

            - name: Check Changes
              id: changes
              run: |
                  git restore '*.test.tsx.snap'
                  if [[ -z "$(git status --porcelain)" ]];
                  then
                    echo "::set-output name=changes::0"
                  else
                    echo "::set-output name=changes::1"
                  fi

            - name: Update Snapshots
              if: steps.changes.outputs.changes == 1
              run: yarn test --maxWorkers=4 --update-snapshot

            - uses: stefanzweifel/git-auto-commit-action@v4
              with:
                  commit_message: "style: resolve style guide violations"
                  branch: ${{ github.head_ref }}

            - name: Test
              run: yarn test:coverage src/domains/setting --forceExit --maxWorkers=4 --coverageDirectory .coverage/unit

    domains-splash:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --frozen-lockfile

            - name: Rebuild
              run: npm rebuild

            - name: Format
              run: |
                  export PATH="$(yarn global bin):$PATH"
                  yarn global add rustywind
                  rustywind --write src

            - name: Check Changes
              id: changes
              run: |
                  git restore '*.test.tsx.snap'
                  if [[ -z "$(git status --porcelain)" ]];
                  then
                    echo "::set-output name=changes::0"
                  else
                    echo "::set-output name=changes::1"
                  fi

            - name: Update Snapshots
              if: steps.changes.outputs.changes == 1
              run: yarn test --maxWorkers=4 --update-snapshot

            - uses: stefanzweifel/git-auto-commit-action@v4
              with:
                  commit_message: "style: resolve style guide violations"
                  branch: ${{ github.head_ref }}

            - name: Test
              run: yarn test:coverage src/domains/splash --forceExit --maxWorkers=4 --coverageDirectory .coverage/unit

    domains-transaction:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --frozen-lockfile

            - name: Rebuild
              run: npm rebuild

            - name: Format
              run: |
                  export PATH="$(yarn global bin):$PATH"
                  yarn global add rustywind
                  rustywind --write src

            - name: Check Changes
              id: changes
              run: |
                  git restore '*.test.tsx.snap'
                  if [[ -z "$(git status --porcelain)" ]];
                  then
                    echo "::set-output name=changes::0"
                  else
                    echo "::set-output name=changes::1"
                  fi

            - name: Update Snapshots
              if: steps.changes.outputs.changes == 1
              run: yarn test --maxWorkers=4 --update-snapshot

            - uses: stefanzweifel/git-auto-commit-action@v4
              with:
                  commit_message: "style: resolve style guide violations"
                  branch: ${{ github.head_ref }}

            - name: Test
              run: yarn test:coverage src/domains/transaction --forceExit --maxWorkers=4 --coverageDirectory .coverage/unit

    domains-vote:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --frozen-lockfile

            - name: Rebuild
              run: npm rebuild

            - name: Format
              run: |
                  export PATH="$(yarn global bin):$PATH"
                  yarn global add rustywind
                  rustywind --write src

            - name: Check Changes
              id: changes
              run: |
                  git restore '*.test.tsx.snap'
                  if [[ -z "$(git status --porcelain)" ]];
                  then
                    echo "::set-output name=changes::0"
                  else
                    echo "::set-output name=changes::1"
                  fi

            - name: Update Snapshots
              if: steps.changes.outputs.changes == 1
              run: yarn test --maxWorkers=4 --update-snapshot

            - uses: stefanzweifel/git-auto-commit-action@v4
              with:
                  commit_message: "style: resolve style guide violations"
                  branch: ${{ github.head_ref }}

            - name: Test
              run: yarn test:coverage src/domains/vote --forceExit --maxWorkers=4 --coverageDirectory .coverage/unit

    domains-wallet:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --frozen-lockfile

            - name: Rebuild
              run: npm rebuild

            - name: Format
              run: |
                  export PATH="$(yarn global bin):$PATH"
                  yarn global add rustywind
                  rustywind --write src

            - name: Check Changes
              id: changes
              run: |
                  git restore '*.test.tsx.snap'
                  if [[ -z "$(git status --porcelain)" ]];
                  then
                    echo "::set-output name=changes::0"
                  else
                    echo "::set-output name=changes::1"
                  fi

            - name: Update Snapshots
              if: steps.changes.outputs.changes == 1
              run: yarn test --maxWorkers=4 --update-snapshot

            - uses: stefanzweifel/git-auto-commit-action@v4
              with:
                  commit_message: "style: resolve style guide violations"
                  branch: ${{ github.head_ref }}

            - name: Test
              run: yarn test:coverage src/domains/wallet --forceExit --maxWorkers=4 --coverageDirectory .coverage/unit
