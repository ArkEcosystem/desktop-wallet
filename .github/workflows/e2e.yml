name: E2E

on:
    push:
        branches:
            - "master"
            - "develop"
    pull_request:
        types: [ready_for_review, synchronize, opened]

jobs:
    build-and-upload:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Build
              run: yarn build:e2e

            - name: Upload Build Artifacts
              uses: actions/upload-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

    votes:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/vote/e2e/votes.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/votes-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/votes-screenshots.tar.gz

    ipfs-transaction:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/transaction/e2e/ipfs-transaction.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/ipfs-transaction-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/ipfs-transaction-screenshots.tar.gz

    ipfs-transaction-multisig:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/transaction/e2e/ipfs-transaction-multisig.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/ipfs-transaction-multisig-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/ipfs-transaction-multisig-screenshots.tar.gz

    single-transfer:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/transaction/e2e/single-transfer.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/single-transfer-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/single-transfer-screenshots.tar.gz

    single-transfer-multisig:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/transaction/e2e/single-transfer-multisig.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/single-transfer-multisig-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/single-transfer-multisig-screenshots.tar.gz

    multiple-transfer:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/transaction/e2e/multiple-transfer.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/multiple-transfer-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/multiple-transfer-screenshots.tar.gz

    transactions-routing:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/transaction/e2e/transactions-routing.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/transactions-routing-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/transactions-routing-screenshots.tar.gz

    delegate-resignation:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/transaction/e2e/delegate-resignation.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/delegate-resignation-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/delegate-resignation-screenshots.tar.gz

    vote-transaction:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/transaction/e2e/vote-transaction.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/vote-transaction-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/vote-transaction-screenshots.tar.gz

    vote-transaction-multisig:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/transaction/e2e/vote-transaction-multisig.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/vote-transaction-multisig-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/vote-transaction-multisig-screenshots.tar.gz

    delegate-registration:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/transaction/e2e/delegate-registration.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/delegate-registration-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/delegate-registration-screenshots.tar.gz

    splash-screen:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/splash/e2e/splash-screen.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/splash-screen-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/splash-screen-screenshots.tar.gz

    contacts-routing:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/contact/e2e/contacts-routing.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/contacts-routing-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/contacts-routing-screenshots.tar.gz

    update-contact:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1200x16" yarn test:e2e src/domains/contact/e2e/update-contact.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/update-contact-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/update-contact-screenshots.tar.gz

    delete-contact:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/contact/e2e/delete-contact.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/delete-contact-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/delete-contact-screenshots.tar.gz

    create-contact:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1200x16" yarn test:e2e src/domains/contact/e2e/create-contact.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/create-contact-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/create-contact-screenshots.tar.gz

    plugins-routing:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/plugin/e2e/plugins-routing.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/plugins-routing-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/plugins-routing-screenshots.tar.gz

    dashboard-routing:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/dashboard/e2e/dashboard-routing.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/dashboard-routing-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/dashboard-routing-screenshots.tar.gz

    navbar-routing:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/dashboard/e2e/navbar-routing.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/navbar-routing-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/navbar-routing-screenshots.tar.gz

    welcome-screen:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/profile/e2e/welcome-screen.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/welcome-screen-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/welcome-screen-screenshots.tar.gz

    create-profile-action:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/profile/e2e/create-profile-action.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/create-profile-action-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/create-profile-action-screenshots.tar.gz

    navigate-profile-settings:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/profile/e2e/navigate-profile-settings.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/navigate-profile-settings-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/navigate-profile-settings-screenshots.tar.gz

    create-profile-routing:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/profile/e2e/create-profile-routing.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/create-profile-routing-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/create-profile-routing-screenshots.tar.gz

    notifications:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/profile/e2e/notifications.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/notifications-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/notifications-screenshots.tar.gz

    delete-profile-action:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/profile/e2e/delete-profile-action.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/delete-profile-action-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/delete-profile-action-screenshots.tar.gz

    news-routing:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/news/e2e/news-routing.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/news-routing-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/news-routing-screenshots.tar.gz

    filtering:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/news/e2e/filtering.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/filtering-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/filtering-screenshots.tar.gz

    create-wallet-action:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/wallet/e2e/create-wallet-action.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/create-wallet-action-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/create-wallet-action-screenshots.tar.gz

    delete-wallet-action:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/wallet/e2e/delete-wallet-action.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/delete-wallet-action-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/delete-wallet-action-screenshots.tar.gz

    wallet-details:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/wallet/e2e/wallet-details.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/wallet-details-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/wallet-details-screenshots.tar.gz

    sign-message-action:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/wallet/e2e/sign-message-action.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/sign-message-action-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/sign-message-action-screenshots.tar.gz

    import-wallet-action:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/wallet/e2e/import-wallet-action.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/import-wallet-action-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/import-wallet-action-screenshots.tar.gz

    verify-message-action:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/wallet/e2e/verify-message-action.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/verify-message-action-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/verify-message-action-screenshots.tar.gz

    wallets-routing:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/wallet/e2e/wallets-routing.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/wallets-routing-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/wallets-routing-screenshots.tar.gz

    update-wallet-name-action:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/wallet/e2e/update-wallet-name-action.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/update-wallet-name-action-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/update-wallet-name-action-screenshots.tar.gz

    save-settings:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/setting/e2e/save-settings.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/save-settings-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/save-settings-screenshots.tar.gz

    reset-profile:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/setting/e2e/reset-profile.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/reset-profile-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/reset-profile-screenshots.tar.gz

    settings-routing:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/setting/e2e/settings-routing.e2e.ts

            - name: Upload Screenshots
              run: tar -czvf ${{ github.workspace }}/settings-routing-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ${{ github.workspace }}/settings-routing-screenshots.tar.gz
