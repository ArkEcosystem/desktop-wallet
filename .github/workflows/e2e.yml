name: E2E

on:
    push:
        branches:
            - "master"
            - "develop"
    pull_request:
        types: [ready_for_review, synchronize, opened]

jobs:
    build-and-upload:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Build
              run: yarn build:e2e

            - name: Upload Build Artifacts
              uses: actions/upload-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

    votes:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/vote/e2e/votes.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf votes-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: votes-screenshots.tar.gz

    ipfs-transaction:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/transaction/e2e/ipfs-transaction.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf ipfs-transaction-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ipfs-transaction-screenshots.tar.gz

    ipfs-transaction-multisig:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/transaction/e2e/ipfs-transaction-multisig.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf ipfs-transaction-multisig-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: ipfs-transaction-multisig-screenshots.tar.gz

    single-transfer:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/transaction/e2e/single-transfer.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf single-transfer-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: single-transfer-screenshots.tar.gz

    single-transfer-multisig:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/transaction/e2e/single-transfer-multisig.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf single-transfer-multisig-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: single-transfer-multisig-screenshots.tar.gz

    multiple-transfer:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/transaction/e2e/multiple-transfer.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf multiple-transfer-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: multiple-transfer-screenshots.tar.gz

    transactions-routing:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/transaction/e2e/transactions-routing.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf transactions-routing-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: transactions-routing-screenshots.tar.gz

    delegate-resignation:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/transaction/e2e/delegate-resignation.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf delegate-resignation-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: delegate-resignation-screenshots.tar.gz

    vote-transaction:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/transaction/e2e/vote-transaction.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf vote-transaction-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: vote-transaction-screenshots.tar.gz

    vote-transaction-multisig:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/transaction/e2e/vote-transaction-multisig.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf vote-transaction-multisig-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: vote-transaction-multisig-screenshots.tar.gz

    delegate-registration:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/transaction/e2e/delegate-registration.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf delegate-registration-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: delegate-registration-screenshots.tar.gz

    splash-screen:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/splash/e2e/splash-screen.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf splash-screen-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: splash-screen-screenshots.tar.gz

    contacts-routing:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/contact/e2e/contacts-routing.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf contacts-routing-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: contacts-routing-screenshots.tar.gz

    update-contact:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1200x16" yarn test:e2e src/domains/contact/e2e/update-contact.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf update-contact-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: update-contact-screenshots.tar.gz

    delete-contact:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/contact/e2e/delete-contact.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf delete-contact-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: delete-contact-screenshots.tar.gz

    create-contact:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1200x16" yarn test:e2e src/domains/contact/e2e/create-contact.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf create-contact-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: create-contact-screenshots.tar.gz

    plugins-routing:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/plugin/e2e/plugins-routing.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf plugins-routing-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: plugins-routing-screenshots.tar.gz

    dashboard-routing:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/dashboard/e2e/dashboard-routing.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf dashboard-routing-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: dashboard-routing-screenshots.tar.gz

    navbar-routing:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/dashboard/e2e/navbar-routing.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf navbar-routing-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: navbar-routing-screenshots.tar.gz

    welcome-screen:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/profile/e2e/welcome-screen.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf welcome-screen-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: welcome-screen-screenshots.tar.gz

    create-profile-action:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/profile/e2e/create-profile-action.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf create-profile-action-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: create-profile-action-screenshots.tar.gz

    navigate-profile-settings:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/profile/e2e/navigate-profile-settings.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf navigate-profile-settings-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: navigate-profile-settings-screenshots.tar.gz

    create-profile-routing:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/profile/e2e/create-profile-routing.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf create-profile-routing-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: create-profile-routing-screenshots.tar.gz

    notifications:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/profile/e2e/notifications.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf notifications-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: notifications-screenshots.tar.gz

    delete-profile-action:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/profile/e2e/delete-profile-action.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf delete-profile-action-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: delete-profile-action-screenshots.tar.gz

    news-routing:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/news/e2e/news-routing.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf news-routing-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: news-routing-screenshots.tar.gz

    filtering:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/news/e2e/filtering.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf filtering-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: filtering-screenshots.tar.gz

    create-wallet-action:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/wallet/e2e/create-wallet-action.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf create-wallet-action-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: create-wallet-action-screenshots.tar.gz

    delete-wallet-action:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/wallet/e2e/delete-wallet-action.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf delete-wallet-action-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: delete-wallet-action-screenshots.tar.gz

    wallet-details:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/wallet/e2e/wallet-details.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf wallet-details-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: wallet-details-screenshots.tar.gz

    sign-message-action:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/wallet/e2e/sign-message-action.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf sign-message-action-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: sign-message-action-screenshots.tar.gz

    import-wallet-action:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/wallet/e2e/import-wallet-action.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf import-wallet-action-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: import-wallet-action-screenshots.tar.gz

    verify-message-action:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/wallet/e2e/verify-message-action.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf verify-message-action-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: verify-message-action-screenshots.tar.gz

    wallets-routing:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/wallet/e2e/wallets-routing.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf wallets-routing-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: wallets-routing-screenshots.tar.gz

    update-wallet-name-action:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/wallet/e2e/update-wallet-name-action.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf update-wallet-name-action-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: update-wallet-name-action-screenshots.tar.gz

    save-settings:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/setting/e2e/save-settings.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf save-settings-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: save-settings-screenshots.tar.gz

    reset-profile:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/setting/e2e/reset-profile.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf reset-profile-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: reset-profile-screenshots.tar.gz

    settings-routing:
        needs: build-and-upload

        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Get yarn cache directory path
              id: yarn-cache-dir-path
              run: echo "::set-output name=dir::$(yarn cache dir)"

            - name: Cache node modules
              uses: actions/cache@v2
              id: yarn-cache
              with:
                  path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
                  key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-yarn-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Download Build Artifact
              uses: actions/download-artifact@v2
              with:
                  name: build
                  path: ${{ github.workspace }}/build

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e src/domains/setting/e2e/settings-routing.e2e.ts

            - name: Pack Screenshots
              if: ${{ failure() }}
              run: tar -czvf settings-routing-screenshots.tar.gz ${{ github.workspace }}/screenshots

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: ${{ github.job }}-screenshots
                  path: settings-routing-screenshots.tar.gz
