name: End-to-End

on:
    push:
        branches:
            - "master"
            - "develop"
    pull_request:
        types: [ready_for_review, synchronize, opened]

jobs:
    contacts-routing:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/contact/e2e/contacts-routing.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    create-contact:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/contact/e2e/create-contact.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    create-profile-action:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/contact/e2e/delete-contact.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    create-profile-routing:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/contact/e2e/update-contact.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    create-wallet-action:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/dashboard/e2e/dashboard-routing.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    dashboard-routing:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/dashboard/e2e/navbar-routing.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    delegate-registration:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/news/e2e/filtering.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    delegate-resignation:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/news/e2e/news-routing.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    delete-contact:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/plugin/e2e/plugins-routing.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    delete-profile-action:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/profile/e2e/create-profile-action.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png
    delete-wallet-action:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/profile/e2e/create-profile-routing.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    filtering:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/profile/e2e/delete-profile-action.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    import-wallet-action:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/profile/e2e/my-registrations-routing.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    multiple-transfer:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/profile/e2e/navigate-profile-settings.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    my-registrations-routing:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/profile/e2e/welcome-screen.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    navbar-routing:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/setting/e2e/save-settings.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    navigate-profile-settings:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/setting/e2e/settings-routing.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    news-routing:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/splash/e2e/splash-screen.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    plugins-routing:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/transaction/e2e/delegate-registration.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    save-settings:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/transaction/e2e/delegate-resignation.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png
    settings-routing:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/transaction/e2e/multiple-transfer.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    sign-message-action:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/transaction/e2e/single-transfer.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    single-transfer:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/transaction/e2e/transactions-routing.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    splash-screen:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/vote/e2e/votes.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    transactions-routing:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/wallet/e2e/create-wallet-action.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    update-contact:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/wallet/e2e/delete-wallet-action.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    update-wallet-name-action:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/wallet/e2e/import-wallet-action.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    verify-message-action:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/wallet/e2e/sign-message-action.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    votes:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/wallet/e2e/update-wallet-name-action.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    wallet-details:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/wallet/e2e/verify-message-action.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    wallets-routing:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/wallet/e2e/wallet-details.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png

    welcome-screen:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v2
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache node modules
              uses: actions/cache@v2
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Update System
              run: sudo apt-get update

            - name: Install (Ledger Requirements)
              run: sudo apt-get install libudev-dev libusb-1.0-0-dev

            - name: Install (Yarn)
              run: yarn install --ignore-engines --frozen-lockfile

            - name: Test
              run: xvfb-run --server-args="-screen 0 1920x1080x16" yarn test:e2e:ci src/domains/wallet/e2e/wallets-routing.e2e.ts

            - name: Upload Screenshots
              if: ${{ failure() }}
              uses: actions/upload-artifact@v2
              with:
                  name: screenshots
                  path: screenshots/**/*.png
