language: node_js
node_js:
    - "node"

os: osx

addons:
  apt:
    packages:
      - xvfb
      - libusb-1.0-0-dev

install:
  - npm install


script:
  # export DISPLAY=:99.0
  # sh -e /etc/init.d/xvfb start
  # sleep 3
  - npm test

cache:
  directories:
    - node_modules
