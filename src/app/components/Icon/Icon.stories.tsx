import React from "react";

import { Icon } from "./Icon";

export default {
	title: "Basic / Icon",
};

export const Default = () => (
	<div className="inline-flex space-x-4">
		<Icon name="Upload" />
	</div>
);
