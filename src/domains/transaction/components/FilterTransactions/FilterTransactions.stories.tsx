import React from "react";

import { FilterTransactions } from "./";

export default {
	title: "Domains / Transaction / Components / FilterTransactions",
};

export const Default = () => (
	<div className="w-128">
		<FilterTransactions />
	</div>
);
