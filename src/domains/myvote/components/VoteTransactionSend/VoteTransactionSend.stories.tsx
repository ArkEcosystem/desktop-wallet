import React from "react";

import { VoteTransactionSend } from "./VoteTransactionSend";

export default { title: "My Votes / Components / VoteTransactionSend" };

export const Default = () => (
	<div className="mt-15">
		<VoteTransactionSend />
	</div>
);
