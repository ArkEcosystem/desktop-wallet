import React from "react";

import { data } from "../../data";
import { AddressList } from "./AddressList";

export default { title: "My Votes / Components / AddressList" };

export const Default = () => <AddressList data={data} />;
