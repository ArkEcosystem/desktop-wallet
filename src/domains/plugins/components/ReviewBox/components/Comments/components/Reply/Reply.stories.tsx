import React from "react";

import { Reply } from "./Reply";

export default { title: "Plugins / Components / Review Box / Components / Comments / Components / Reply" };

export const Default = () => <Reply />;
